{
  "name": "autobbq",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "cargo build --manifest-path backend-rs/Cargo.toml --release && npm run build -w frontend",
    "test": "cargo test --manifest-path backend-rs/Cargo.toml && npm run build -w frontend"
  },
  "engines": {
    "node": ">=22.5.0",
    "npm": ">=10"
  }
}
